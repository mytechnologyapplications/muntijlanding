import { ReactNode } from "react";

export interface FeatureCardProps {
  icon: ReactNode;
  title: string;
  description: string;
}

export interface Feature {
  icon: ReactNode;
  title: string;
  description: string;
}
